left: 2i+1
right: 2i+2
parent: (i-1)//2


only care about ints right now (heap runner only accepts int)
make a arraylist, private int arraylist.

private
.left(int index) - gets the left child of the index
.right(int index) - gets the right child of the index
.parent(int index) - gets the parent of the index.

public
null .add(int num)
null .pop().
int .peek() - grab last index
int .size() - just return the size of the internal arraylist
boolean .empty() - if arraylist has anything (.size()) return true/false)